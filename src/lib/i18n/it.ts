// Italian translations bootstrap.
// Initial content mirrors the Spanish dictionary so the UI keeps working
// while professional IT copy is finalized. Replace strings entry-by-entry.
import { es } from './es';

export const it = es;
