// German translations bootstrap.
// Initial content mirrors the Spanish dictionary so the UI keeps working
// while professional DE copy is finalized. Replace strings entry-by-entry.
import { es } from './es';

export const de = es;
